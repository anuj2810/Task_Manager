databases:
  - name: inzint-db
    plan: free

services:
  - type: web
    name: inzint-backend
    runtime: python
    plan: free
    rootDir: backend
    buildCommand: pip install -r requirements.txt && python manage.py migrate && python manage.py collectstatic --noinput
    startCommand: gunicorn core.wsgi:application --bind 0.0.0.0:8000
    envVars:
      - key: DEBUG
        value: "false"
      - key: SECRET_KEY
        value: "PLEASE_SET_IN_RENDER_DASHBOARD"
      - key: ALLOWED_HOSTS
        value: "PLEASE_SET_BACKEND_HOST"
      - key: CORS_ALLOWED_ORIGINS
        sync: false
      - key: CSRF_TRUSTED_ORIGINS
        sync: false
      - key: GOOGLE_CLIENT_ID
        value: "PLEASE_SET_GOOGLE_CLIENT_ID"
      - key: DATABASE_URL
        fromDatabase:
          name: inzint-db
          property: connectionString

  - type: web
    name: inzint-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        value: "https://PLEASE_SET_BACKEND_HOST/api"
      - key: VITE_GOOGLE_CLIENT_ID
        value: "PLEASE_SET_GOOGLE_CLIENT_ID"